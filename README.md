# My Todos

### Backend

1. Navigate to the `server` directory:

   cd server

2. Install all the dependencies:

   npm install

3. Start the server:

   npm start

### Frontend

1. Navigate to the `client` directory:

   cd client

2. Install all the dependencies:

   npm install

3. Run frontend code:

   npm run dev
